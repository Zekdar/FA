algorithme remplissage
	constantes
		TAILLE_TABLEAU = 42
  types
  	tableauEntiers = TAILLE_TABLEAU entier
  variables
  	tableauEntiers tableau
  	entier i
debut
	pour i ← 1 jusqu'a TAILLE_TABLEAU faire
		tableau[i] ← i
	fin pour

	i ← 1

	tant que i <= 41 faire
		afficher(chaine(tableau[i]) + " ")
		i ← i + 1
	fin tant que

	afficher(chaine(tableau[TAILLE_TABLEAU]) + "\n")
fin algorithme remplissage
